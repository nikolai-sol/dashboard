import type { DatasetMeta } from "@reportingdash/site-seo-contract";
import type { SeoOsCanonicalData } from "../lib/db.ts";
export function SeoOs({ id, meta, data }: Readonly<{ id: string; meta?: DatasetMeta; data: SeoOsCanonicalData | null }>) { return <section id={id}><h2>SEO OS</h2><p>{meta?.state === "missing" ? "Нужна опубликованная выгрузка" : meta?.state === "partial" ? "Сигналы опубликованы с ограничениями" : "Сигналы доступны"}</p><table><tbody>{data?.recommendations.map((row, index) => <tr key={`${row.topic ?? row.kind ?? "recommendation"}:${index}`}><th>{row.topic ?? row.kind ?? "Рекомендация"}</th><td>{row.action ?? "нет действия"}</td><td>{row.pageUrl ?? "нет страницы"}</td><td>rule: {row.ruleVersion ?? "нет"}; периоды: {row.sourcePeriods.join(", ") || "нет"}; sources: {row.sourceIds.join(", ") || "нет"}</td><td>{row.publicationStatus ?? "нет статуса"}</td></tr>)}</tbody></table><ul>{data?.tasks.map((task) => <li key={task.id}>{task.id}: {task.status}</li>)}</ul></section>; }
