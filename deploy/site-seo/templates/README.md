# Site SEO release templates

These templates are data-only release inputs. A site profile supplies the
route, asset prefix, process, port, deploy path, and release branch. The
release scripts never inherit Zaruku/Abbott paths and never activate source
collectors as part of a web release.
