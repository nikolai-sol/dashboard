# Abbott Task 8 Review Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close every remaining Task 8 review finding while preserving append-only Abbott release construction, read-only content validation, and the existing reviewed `staging -> validated` transition.

**Architecture:** Keep `candidate_release.py` as the single content-bundle attestor, but expose an internal connection-owned inspection path so the production canonical control comparator can generate the eleven `content.*` evidence rows in the same validation transaction. Resolve first-successor legacy provenance only from the locked active predecessor, active strong aliases, and the accepted batch taxonomy; stream deterministic non-content hashes by declared natural grains.

**Tech Stack:** Python 3.11, MySQL 8 SQL, `unittest`, Next.js TypeScript contract tests.

## Global Constraints

- External APIs remain collector-only; all materialization and validation reads canonical MySQL.
- Migration 033 is immutable; migration 046 remains additive and repeat-safe.
- Never select unactivated global classification events for legacy provenance.
- `validate_content_candidate` remains read-only and never activates a release.
- No deployment, migration execution, grants execution, secret/API/cron/Telegram/Hermes action, or live database operation.
- Preserve the two runtime/bootstrap checksum failures for Task 10 only.

---

### Task 1: Schema-accurate taxonomy validation

**Files:**
- Modify: `tests/abbott_page_classifier/test_candidate_release.py`
- Modify: `tests/test_abbott_schema_contract.py`
- Modify: `agents/abbott_page_classifier/candidate_release.py`

**Interfaces:**
- Consumes: `portal_content_taxonomy_terms.term_status` from migration 045.
- Produces: validation SQL containing `term_status = 'active'` and no taxonomy-term `is_active` reference.

- [ ] Add a strict fake that parses taxonomy queries and raises on columns not present in migration 045; assert the exact `term_status = 'active'` token and absence of `is_active`.
- [ ] Run the focused tests and observe the expected unknown-column/SQL-token RED.
- [ ] Change the taxonomy query to select `term_status` and filter active terms; bind immutable referenced code/label mappings into the candidate bundle so already-reviewed referenced terms can be attested without consulting unrelated taxonomy versions.
- [ ] Run the focused candidate/schema tests GREEN.

### Task 2: Production content control evidence and reviewed transition

**Files:**
- Modify: `tests/test_abbott_canonical_controls.py`
- Modify: `tests/test_canonical_release_store.py`
- Modify: `abbott_canonical_controls.py`
- Modify: `agents/abbott_page_classifier/candidate_release.py`

**Interfaces:**
- Consumes: `_inspect_content_candidate(connection, candidate_release_id, expected_counts, accepted_hash) -> GateReport` and baseline `content_candidate_bundle`.
- Produces: eleven persisted `portal_migration_validation_runs` rows named exactly as `CONTENT_CONTROL_VALUES`, generated by `compare_release_control_pack` before `canonical_release_store.validate_release` transitions the release.

- [ ] Add a production-path test whose comparator fake invokes `compare_release_control_pack`, captures its inserted evidence, then feeds those exact rows to `canonical_release_store.validate_release`; assert all eleven content controls are present and the active pointer is untouched.
- [ ] Run the new test and observe RED because `_control_values` cannot produce `content.*` controls.
- [ ] Extract the connection-owned content inspection core from the read-only wrapper; have `compare_release_control_pack` call it and map the `GateReport` to the eleven numeric candidate values.
- [ ] Run canonical-control and release-store tests GREEN.

### Task 3: Secure first-successor legacy provenance

**Files:**
- Modify: `tests/abbott_page_classifier/test_candidate_release.py`
- Modify: `dashboard-next/src/db/migrations/046_abbott_content_candidate_provenance.sql`
- Modify: `agents/abbott_page_classifier/candidate_release.py`

**Interfaces:**
- Consumes: locked predecessor catalog rows, active strong `material_id`/URL aliases, accepted batch taxonomy terms, and visible active-catalog labels.
- Produces: `CandidateCatalogRow` with nonzero entity identity, canonical taxonomy codes, `legacy_active_catalog_baseline` provenance, predecessor row identity, and a complete row hash.

- [ ] Add realistic all-null pre-046 predecessor rows plus exact strong-alias and taxonomy resolver rows; add ambiguous/unmapped cases that must fail closed and assert no global latest-event query.
- [ ] Run the new tests and observe `PREDECESSOR_PROVENANCE_INVALID` RED.
- [ ] Add a resolver query scoped to the locked predecessor release/snapshot and accepted taxonomy version. Require exactly one entity from active strong aliases and exactly one code for every visible label/lifecycle state; derive append-only successor baseline provenance without updating legacy rows.
- [ ] Clarify migration 046's repeat-safe handoff contract in SQL comments and keep it free of unsafe `UPDATE` backfills.
- [ ] Run materialization and migration/schema tests GREEN.

### Task 4: Deterministic streamed non-content bundles

**Files:**
- Modify: `tests/abbott_page_classifier/test_candidate_release.py`
- Modify: `agents/abbott_page_classifier/candidate_release.py`

**Interfaces:**
- Consumes: `_NON_CONTENT_RELEASE_TABLES` entries containing `(table, columns, natural_grain)`.
- Produces: `{table: {count, hash}}` generated incrementally with `fetchmany`, and `INSERT ... SELECT ... ORDER BY <natural grain>` copies.

- [ ] Add a fake that raises if `fetchall` is used for any non-content fact query, yields multiple chunks through `fetchmany`, and asserts each read/copy SQL orders by the declared canonical unique grain rather than `id`.
- [ ] Run the new test and observe the expected `fetchall`/`ORDER BY id` RED.
- [ ] Add an incremental canonical JSON array hasher and natural-grain metadata for all thirteen public/private tables; stream reads in bounded chunks and use the same grain for copying and rehashing.
- [ ] Run materialization, corruption, and memory-bound contract tests GREEN.

### Task 5: Approval and snapshot completeness

**Files:**
- Modify: `tests/abbott_page_classifier/test_candidate_release.py`
- Modify: `agents/abbott_page_classifier/candidate_release.py`

**Interfaces:**
- Consumes: scalar `conflict_code`, JSON `conflict_codes`, and snapshot `source_row_count`.
- Produces: fail-closed approval consistency and snapshot/file hashes that include source row counts.

- [ ] Add tests where scalar conflict differs from the first JSON code and where only `source_row_count` changes.
- [ ] Run both tests RED.
- [ ] Select and compare `conflict_code` exactly (`NULL` iff list empty, otherwise first code), and add `source_row_count` to all snapshot selects, records, file manifests, and hashes.
- [ ] Run candidate and release-source tests GREEN.

### Task 6: Complete dashboard smoke attestation

**Files:**
- Modify: `tests/abbott_page_classifier/test_candidate_release.py`
- Modify: `agents/abbott_page_classifier/candidate_release.py`
- Verify: `dashboard-next/src/lib/abbott-private-store.test.ts`

**Interfaces:**
- Consumes: candidate-scoped catalog and lookup projections.
- Produces: smoke counts for eligible catalog rows, resolved candidate rows, title/slug/path resolution, and nonblank direction/material/access labels on the same joined rows.

- [ ] Add a fake returning two eligible candidate rows but only one resolved projection; assert the smoke gate fails, and assert candidate release/snapshot filters plus Russian labels are checked.
- [ ] Run the new test RED because the current gate accepts any one title row.
- [ ] Replace the smoke scalar with candidate-scoped aggregate counts and require exact eligible/resolved parity plus nonzero title/slug/path coverage and label counts.
- [ ] Run candidate and dashboard contracts GREEN.

### Task 7: Verification, commits, and report

**Files:**
- Modify: `.superpowers/sdd/task-8-report.md` (ignored administrative report)
- Verify all files above plus grants and migrations.

**Interfaces:**
- Consumes: all GREEN evidence.
- Produces: clean root/nested commits and exact verification report.

- [ ] Run focused candidate, canonical-controls, release-store, schema, grants, migration, rehearsal, and dashboard suites.
- [ ] Run `.venv/bin/python -m unittest discover -s tests`; confirm only the two Task 10 manifest failures remain.
- [ ] Run compile, diff-check, immutable-033 diff, and root/nested worktree checks.
- [ ] Commit nested dashboard changes first, then the root gitlink/implementation changes.
- [ ] Refresh `.superpowers/sdd/task-8-report.md` with RED/GREEN evidence, exact commits, files, counts, and the remaining Task 10 checksum concern.

## Self-review

- Spec coverage: all eight findings map to Tasks 1–6; verification/reporting maps to Task 7.
- Placeholder scan: no deferred implementation steps or unspecified error handling remain.
- Type consistency: the connection-owned inspector returns the existing `GateReport`; control generation consumes the existing baseline bundle and emits existing `ControlResult`/validation-row shapes.

## Execution

The parent task explicitly requested immediate implementation, so this session will use inline execution with the `executing-plans` skill and checkpoint after each RED→GREEN slice.
