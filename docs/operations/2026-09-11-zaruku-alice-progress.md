# Compact Alice history execution ledger

Plan: `docs/superpowers/plans/2026-09-11-zaruku-alice-compact-history.md`.

- Task 1: complete (`a89c7b1..91d30e4`, independent spec/quality review approved).
- Minor review item: two-point separation 128 px versus approximate 100–120 target; no clipping or spread across card. Final review to triage.
- Task 2: in progress. Browser 16-case matrix and filter/pagination checks pass. Release predecessor pin tests: RED 14/15, GREEN 15/15. Final independent review approved without required corrections.
- Source worktree: `/Users/nafanya/ReportingDash/dashboard-next/.worktrees/zaruku-production-release`.
- Build clone: `/Volumes/Elements/zaruku-alice-release.UNFhQL/source` (external disk due local ENOSPC).
- External typecheck and isolated build passed at `8a5f25e` (same production code as final candidate).
- Final candidate `6650c321f5575b43593ae150a4ac9a210457519f` differs only by a factual operations-note correction.
- Exact successor advance from `ef9ec05` succeeded, exact remote check passed, 22-file reviewed control staged (digest `335df3a44336715d842d2513bf6854760544b6b63296c3652ee4785d2c208639`).
- First isolated deployment run `af61410e-e7df-4820-82ca-652ab6e3b731` (exec `35691`) refused after all predeploy tests/builds passed. `preview-builder:test` left `scripts/__pycache__/copy-preview-tree.cpython-314.pyc`, making source dirty. Live Zaruku remains `ef9ec05`; shared SHA and Nginx hash unchanged.
- Reproduced the source guard refusal, moved only generated bytecode to `/Volumes/Elements/zaruku-alice-release.UNFhQL/python-cache-run1`, then verified clean source. Re-running preview-builder tests with `PYTHONDONTWRITEBYTECODE=1` passed (7 Python tests and shell tests) and left source clean. No ignore rule or safety check was weakened.
- Explicit isolated boot/health and payload preparation passed for final candidate (2875 manifest files). Full deploy retry uses the same script plus `PYTHONDONTWRITEBYTECODE=1`; all gates will run again.
- Retry run `61a943e1-1c16-4139-bd5c-8eb20c43bc9a`, former exec session `69431`, is no longer running after session recovery. Its final process result was unavailable. npm logs confirm root build and final `preview-builder:test` exited 0 (12:57:45 and 13:04:02 UTC logs). Do not infer successful deployment from those checks or repeat the already-successful release-ref advancement.
- Fresh server read after the owner's status question: live Zaruku still `ef9ec05`, shared SHA/PID and Nginx hash unchanged; health passes; release staging directory empty and deploy lock clear. No September 11 release record exists. Exact stop reason remains under diagnosis. Read-only local boot/payload diagnostic runs as exec `88715`; source check passed for `6650c321`.
- No live application publication confirmed yet. No DB/auth/collector/cron or non-Zaruku changes.
- Codex internal browser is blocked by the locked Mac; final live UI check requires manual unlock. Do not claim this check passed.

## Completion retry after owner request «добей тогда»

- Read-only diagnostic `88715` ended with `Zaruku standalone boot/health check failed`, identifying the post-build boot gate rather than transport as the stopping point. Direct credential-free boot on the external USB disk then answered exact health in 12,045 ms, close to the unchanged 15,000 ms gate; source and manifest validation did not fail.
- Internal disk now has 4.3 GiB free. Clean independent local clone `/Users/nafanya/zaruku-alice-final.OvpMBr/source` retains exact candidate `6650c321` and exact GitHub origin. No timeout or safety gate change. Comparing the same full release pipeline on internal storage.
- Initial launcher in this fresh clone required `tsx` before its own npm-ci stage, so bootstrap dependencies are being installed from the existing cache (`npm ci --offline --no-audit`, session `25810`). The release itself will still execute its original full npm-ci and all verification gates. No server mutation from this bootstrap failure.
- Bootstrap completed (932 packages, 56 seconds). Full unchanged deployment is running in local clone, run `0cdd6816-40b2-475c-b880-a44e88f780c9`, exec `94141`. Durable script output: `/Users/nafanya/zaruku-alice-final.OvpMBr/release-local.log`. Standard npm-ci completed and predeploy started. Preserve the clean build clone while it runs.
- Local full pipeline passed all predeploy gates and boot, then timed out in the 300-second SSH transfer. No server release was staged; live predecessor and clear lock were rechecked. Thus two environmental stages failed independently: USB cold boot and uncompressed network transfer.
- Same-host 5 MiB synthetic transport reproduction: unchanged SSH timed out after 30,003 ms; adding only `-C` transferred all 5,242,880 bytes in 1,456 ms. No files or credentials were transmitted in that diagnostic.
- Minimal source fix adds only `-C` to the Zaruku deploy transport, preserving the host, strict host-key check, environment, payload digest, dispatcher, size limits, timeout, and activation gates. No SSH user config changes. Exact invocation regression and successor-pin tests failed first, then passed 2/2. The approved predecessor pin now names remote `release/zaruku` candidate `6650c321` (never activated); the actual live ancestor remains `ef9ec05` and must still be included.
