# Abbott local rollout rehearsal — 2026-07-17

This rehearsal was local-only. It did not call Yandex APIs, connect to a production database, install cron entries, send Telegram messages, or invoke Hermes.

## Verified locally

- MySQL image: `mysql:8.4.10` with resolved digest `sha256:c592c15aaf4a1961e15d82eb31ea5987dda862d1c4b1e93424438c0e91dc1f8d`.
- Fresh migration chain: 36 tracked migrations through `033`, followed by the private Abbott schema and grants.
- The migration chain is repeat-safe in the ephemeral database.
- Source Bitrix dump schema: 38 base tables; schema-only load completed with `sql_error_class=none`. No dump rows were loaded.
- Application schema signature: `f3c51803f564bdce70992611518b994bc25a28d14853d5b8cb026d10ad561c21`.
- Grant signature: `c7ba75269847bb8188b1408e25c811e644464ae600f42a0aa972d326f0f8f2f2`.
- MySQL-safe roles were exercised with live allow/deny probes for collector, importer, release operator, and runtime reader.
- The protected workbook audit preserved 1,769 source rows, including worksheet and source-row provenance. Aggregate audit results were 1,469 rows with slugs, 1,828 lookup keys, 336 identical groups, and 945 ambiguous groups.
- After the streaming skip path was hardened to track SQL quote/comment state across multiline rows, a fresh full 8.1 GiB pass completed in 147.83 seconds with maximum resident memory below 13 MiB. No filtered output was retained by this performance check.

Private source checksums remain only in protected uncommitted evidence. They are intentionally not copied into tracked documentation.

## Correct fail-closed result

The four-source import and release lifecycle were not completed. The temporary Bitrix JSON exports are an older exploratory format:

- the page export is `normalized_url`-grain and has no per-row `report_date` or completeness manifest;
- the journey export contains session-level paths rather than ordered event-grain rows and has no completeness manifest.

The canonical importer rejected these files before committing source snapshots. No candidate was activated and no rollback was required. Creating synthetic completeness claims or inferred journey events would make the rehearsal evidence misleading, so no conversion was fabricated.

The Bitrix files are test fixtures only. Source mapping, completeness rules, incremental extraction, stable identifiers, and scheduling must be designed after read-only access to the live Bitrix database is available.

## Production gates still closed

- owner token and production database credentials are not provisioned;
- the known Metrika gap for `2026-03-29..2026-04-07` has not been downloaded;
- legacy/canonical cron changes, Telegram summary delivery, monitoring alerts, and Hermes scheduling have not been installed;
- secret rotation and release activation require a reviewed owner/DBA handoff.
